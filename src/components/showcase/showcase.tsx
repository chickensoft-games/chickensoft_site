import useGlobalData from '@docusaurus/useGlobalData';
import React from 'react';

export type ShowcaseProps = {};

export default function GithubCard(props: ShowcaseProps): JSX.Element {
  const data = useGlobalData();

  return <div></div>;
}
