---
title: ⏳ Countdown
description: Making the timer count down.
image: /docs/logic_blocks/header.jpg
---

Now that our timer can be turned on, off, and set to a given duration, it's time to make it actually work.

We could build the timer in a really simple fashion, but in the interest of actually learning something, let's pretend that our project has a clock service that exposes an [event] that informs us when a second has passed, roughly speaking.

In this section, we'll make our new `Countdown` state subscribe to the clock service and respond to a new `TimeElapsed` input.

First, let's define what the clock service looks like.

```csharp
/// <summary>
/// A service that announces the passage of time, roughly once per second.
/// </summary>
public interface IClock {
  /// <summary>
  /// Invoked about every second or so. Provides the time since the last
  /// invocation (in seconds).
  /// </summary>
  event Action<double> TimeElapsed;
}
```

Easy enough. Let's make a new `Countdown` state. We can register [entrance and exit callbacks][states] to subscribe to the clock service whenever `Countdown` becomes the active state type, and unsubscribe when we leave it.

```csharp
public static class Input {
  public readonly record struct PowerButtonPressed;
  public readonly record struct ChangeDuration(double Duration);
  public readonly record struct StartStopButtonPressed;
  public readonly record struct TimeElapsed(double Delta);
}

public record Countdown : PoweredOn, IGet<Input.TimeElapsed>, IGet<Input.StartStopButtonPressed> {
  public Countdown() {
    this.OnEnter(() => Get<IClock>().TimeElapsed += OnTimeElapsed);
    this.OnExit(() => Get<IClock>().TimeElapsed -= OnTimeElapsed);
  }

  private void OnTimeElapsed(double delta) =>
    Input(new Input.TimeElapsed(delta));

  public Type On(in Input.TimeElapsed input) {
    var data = Get<TimerLogic.Data>();
    data.TimeRemaining -= input.Delta;
    return data.TimeRemaining <= 0.0d ? To<Beeping>() : ToSelf();
  }

  public Type On(in Input.StartStopButtonPressed input) => To<Idle>();
}
```

We access the clock service through the blackboard in `OnEnter` and `OnExit` so that we can subscribe and unsubscribe to it when the `Countdown` state type is entered and exited.

We've also added a private method in `Countdown` that adds an input to the logic block whenever the clock invokes the `TimeElapsed` event.

```csharp
private void OnTimeElapsed(double delta) =>
  Input(new Input.TimeElapsed(delta));
```

The `Input()` method allows us to add an input on the logic block we are attached to. Since we unsubscribe when we're exiting, this method will only ever get called when the `Countdown` state is active.

<Callout type="tip">
By subscribing to an event in `OnEnter` and adding an input in the event handler, we can force a state change to occur reactively. This pattern allows logic blocks to respond to external events without creating a mess of your code.
</Callout>

<Callout type="note">
The `OnEnter` and `OnExit` callbacks respect the type hierarchy. `Countdown.OnEnter` fires whenever we transition **into** the `Countdown` type (from any non-`Countdown` state). `Countdown.OnExit` fires whenever we leave the `Countdown` type (transitioning to any non-`Countdown` state). This is exactly the behavior we want for managing the clock subscription.
</Callout>

Don't forget to also register the new `Countdown` state in the logic block constructor:

```csharp
public TimerLogic() {
  Set(new TimerLogic.Data() { Duration = 30.0d });
  Set(new TimerState.PoweredOff());
  Set(new TimerState.PoweredOn.Idle());
  Set(new TimerState.PoweredOn.Countdown()); // add this

  // Also add the clock service
  Set<IClock>(new MyClock());
}
```

Finally, in our input handler for the `TimeElapsed` input, we decrement the time remaining and transition to the `Beeping` state when the time runs out.

We don't have a beeping state yet, so we'll go ahead and create one in the next section.

[event]: https://learn.microsoft.com/en-us/dotnet/csharp/programming-guide/events/
[states]: /docs/logic_blocks/basics/states#-lifecycle-callbacks
